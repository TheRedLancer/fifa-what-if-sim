# FIFA What-If Sim

Static React simulator for FIFA World Cup 2026 group-stage what-if results.

## Local development

```bash
npm install
npm run dev
```

## Deployment

Pushing to `main` runs `.github/workflows/deploy.yml`, builds the Vite app, and deploys `dist` to GitHub Pages.
